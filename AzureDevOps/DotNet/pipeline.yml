
parameters:
  buildConfiguration: "Release"
  pool: "ubuntu-latest"

stages:
  - stage: Build
    pool: ${{ parameters.pool }}
    jobs:
      - job: "DotNet"
        steps:
          - checkout: self
            submodules: recursive

          - template: "./build.yml"

      - job: "Docker"
        template: "../Docker/build.yml"
